def max_num(numbers):
    # base case
    if number == 0:
        return 0
        
    # init
    max = numbers[0]
    
    # recursive case
    # Recurse though the array fo find the largest number
    
    equation = max_num((n-1) + (n-2))
    return equation